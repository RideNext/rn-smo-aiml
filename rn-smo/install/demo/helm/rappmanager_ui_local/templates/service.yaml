apiVersion: v1
kind: Service
metadata:
  name: {{ include "rappmanager.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rappmanager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "rappmanager.selectorLabels" . | nindent 4 }}
